<%- model_class = Participacao -%>
<p>Participações </p><br>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_participacao_path,
            :class => 'btn btn-primary' %>
<table class="table table-bordered" >
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:dialog) %></th>
      <th><%= model_class.human_attribute_name(:titulo) %></th>
      <th><%= model_class.human_attribute_name(:imagem) %></th>
      <th><%= model_class.human_attribute_name(:texto) %></th>
      <th><%= model_class.human_attribute_name(:soundcloud_item) %></th>
      <th>Social Soundcloud.com (<%= model_class.human_attribute_name(:url_code_share) %>)</th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @participacaos.each do |participacao| %>
      <tr>
        <td><%= participacao.dialog %></td>
        <td><%= participacao.titulo %></td>
        <td><%= participacao.imagem %></td>
        <td><%= participacao.texto %></td>
        <td><%= participacao.soundcloud_item %></td>
        <td><%= participacao.url_code_share %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_participacao_path(participacao), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      participacao_path(participacao),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
